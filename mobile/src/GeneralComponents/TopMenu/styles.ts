import styled from "styled-components/native";
import { GlobalColors } from "../../Global";

export const Container = styled.View`
  background: ${GlobalColors.secundary};
`;

export const Title = styled.Text`
  font-size: 24px;
  margin-left: 5px;

  padding-top: 10px;
  padding-bottom: 10px;
`;
